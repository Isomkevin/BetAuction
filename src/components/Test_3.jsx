import { Box, Button } from '@chakra-ui/react';

function MyComponent() {
  return (
    <Box className="p-4 bg-gray-200 rounded">
      <Button colorScheme="teal" className="mt-2">
        Chakra Button with Tailwind Margin
      </Button>
    </Box>
  );
}

export default MyComponent;
